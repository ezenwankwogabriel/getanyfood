/* eslint-disable no-undef */
describe('Authentication Middlewares', () => {
  describe('Validate Email on forgot password', () => {
    it('should return 403 if not user was found', () => {
    });
  });
});
